<div id="follow_form">
	<% if current_user != @user %>
	<% if current_user.following?(@user) %>
	<%= render 'tweeets/unfollow' %>
	<% else %>
	<%= render 'tweeets/follow' %>
	<% end %>
	<% end %>
</div>